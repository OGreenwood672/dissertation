\label{sec:4}

Compelling argument demonstrating success criteria met, or well-justified explanation of different direction taken.
Excellent critical thought and interpretation which substantiate any claims of success.

\section{Evaluation Setup}
\label{sec:4.1}

For the following evaluations, a specific cooperative simulation setup is created.
There exist two agents.
Each agent has two ingredients to collect from unique stations.
They also each have a station to drop off their product.
Each agent cannot observe their own pick up stations, but can observe their teammates.
This setup ensures cooperation be required and therefore communication must emerge for optimal task reward.

\section{System Performance}
\label{sec:4.2}

Reinforcement Learning with multiple agents requires millions of datapoints for updating the polcies and an equal number to evaluate the language and policy.
This section quantitivley measures the performance gains through the custom systems developed.
Specifically, it analyses the steps per second of the rust simulation developed and the memory efficiency of the \lstinline[language=Python]|RolloutBuffer|.
Benchmarks were evaludated on NVIDIA RTX 5060 Ti and AMD Ryzen 7 3700X.

\subsection{Vectorised Environment Throughput}
\label{sec:4.2.1}

A major issue with Python running multiple threads concurrently is the GIL.
While Python's builtin library \lstinline[language=Python]|multiprocessing| can, in theory, bypass this limitation, it introduces considerable overhead.
For this RL project, all threads require synchronisation at every simulation timestep.

To justify developing the simulation in Rust rather than Python, a simple mock simulation was developed in Python.
This mock simulation was compared to the vectorised Rust backend.
Environment steps per second (SPS) was calculated to analyse the throughput of each of the simulations.
Number of worlds parallised was scaled ($W \in \{2^0, 2^1, ..., 2^{11}\}$) to measure concurrency efficiency.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{sim-comparison.png}
    \caption{Concurrent Environment Throughput Performance. Comparison of Python multiprocessing and Rust with Rayon across an exponentially increasing number of parallel worlds}
    \label{fig:sim-time-comparison}
\end{figure}

As shown in figure \ref{fig:sim-time-comparison}, the Python baseline completely fails to match the Rust simulation.
Despite being able to bypass the GIL and using multiple CPU cores, the serialisation and the Inter-Process Communication overhead required to transfer the necassary data between isolated processes at every time step heavily bottleneck the throughput.

In contrast, the Rust is able utilise \lstinline[language=Rust]|Rayon|'s thread-pool to efficiently manage the parallel world steps nativly in C-space.
After a minor scheduling overhead at $W=2$, throughput scales linearly with number of worlds parallised in $\log_2$.
By using Rust's native Rayon threading, the sequential fraction of the program is minimised per Amdahl's Law allowing near-perfect scaling that saturates the CPU cores.

\subsection{Rollout Buffer Efficiency}

A secondary major potential bottleneck is transferring the results of the rollout phase to the training phase (across CPU-GPU boundary).
To evaluate the \lstinline[language=Python]|RolloutBuffer|, the memory allocation and transfer overhead is compared to a baseline method (appending to Python lists and executing \lstinline[language=Python]|torch.stack| after the rollout phase).

\begin{table}[h]
    \centering
    \begin{tabular}{|l|c|c|c|}
    \hline
    \textbf{Memory Strategy} & \textbf{Allocation Time (s)} & \textbf{Peak VRAM Overhead (MB)} \\ \hline
    \textbf{Dynamic Python Lists} & $6.47 \pm 0.18$ & $1213.45$ \\ \hline
    \textbf{Pre-allocated RolloutBuffer} & $6.79 \pm 0.23$ & $598.23$ \\ \hline
    \end{tabular}
    \caption{Memory Management Overhead per Epoch: $T=20,000$, $W=32$}
    \label{tab:rolloutbuffer-eval}
\end{table}

As shown in \ref{tab:rolloutbuffer-eval}, the \lstinline[language=Python]|RolloutBuffer| method reduces VRAM by over $50\%$.
This is as Python's dynamic arrays often require copying their contents from one memory location to another to increase their size.
Whilst there is a $\sim 5\%$ improvement in allocation time when using Python's dynamic list, the large improvement in VRAM will enable scaling this method to millions of datapoints.


\section{Offline Pre-training}
\label{sec:4.3}

Before we are able to integrate the language into reinforcement learning, its health must be evaluated.
A poorly initialised frozen encoder could limit the agents' language considerably, preventing them from reaching their maximum cooperation potential.

\subsection{Discretization Stability: VQ-VAE vs. SQ-VAE}
\label{sec:4.3.1}

For this evaluation, reconstruction loss and commitment loss are tracked throughout a training process of 50 epochs using 26880 datapoints.
Liu et al. defined a healthy encoder for the AI Mother Tongue system.
Reconstruction loss should fall between 0.08 and 0.15.
Commitment Loss must remain below 1.0.

For this evaluation, reconstruction loss are tracked throughout training processes for VQ-VAE and SQ-VAE.
For VQ-VAE, commitment loss is also tracked.
Liu et al. defined a healthy encoder for the AI Mother Tongue system.
Reconstruction loss should fall between 0.08 and 0.15.
Commitment Loss must remain below 1.0.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{recon_loss_eval.png}
    \caption{Reconstruction Loss over 50 epochs comparing VQ-VAE and SQ-VAE}
    \label{fig:vae_training_eval}
\end{figure}

As shown in \ref{fig:vae_training_eval}, both methods of training got below the maximum reconstruction loss of 0.15.
However, training using SQ-VAE lead to a final reconstruction off of below 0.08.
Liu et al.'s recommended loss for a healthy codebook is based on low reconstruction often being a symptom of 'mode collapse'.
This is where the network memorises a small set of generalisable states.

To determine if this 'mode collapse' has occurred, the codebook utilisation was analysed.

\begin{table}[h]
    \centering
    \begin{tabular}{|l|c|c|}
    \hline
    \textbf{} & \textbf{VQ-VAE} & \textbf{SQ-VAE} \\ \hline
    \textbf{Codebook Utilisation} & $66.67\% \pm 4.8\%$ & $94.8\% \pm 6.5\%$ \\ \hline
    \end{tabular}
    \caption{Codebook utilisation comparison between training with VQ-VAE and SQ-VAE}
    \label{tab:codebook_util}
\end{table}

The table \ref{tab:codebook_util} clearly shows using a SQ-VAE, though having a low reconstruction loss, in fact has a better codebook utilisation than using a VQ-VAE.
Therefore, this low loss indicates high effectiveness in the model rather than 'mode collapse'.

By the end of the training the VQ-VAE, the commitment loss has converged to $\sim 0.25$.
This is well below Liu et al.'s requirement.

\subsection{Codebook Initialisation}
\label{sec:4.3.2}

% A potential concern of using a VQ-VAE is the previously discussed 'mode collapse'.
% The 'mode collapse' effectivly reduces the vocabulary size of the language restricting the expressiveness of the agents.
% To evaluate this concern, an ablation study is performed.
% Two initialisation strategies are compared:
% a standard uniform random initalisation,
% and a data-driven K-Means prior.

% \begin{table}[h]
%     \centering
%     \begin{tabular}{|l|c|c|c|}
%     \hline
%     \textbf{Initialisation Strategy} & \textbf{Final Validation Recon Loss} & \textbf{Active Codes} \\ \hline
%     \textbf{Random Uniform Prior} & $0.064 \pm 0.01$ & $79.13 \pm 12.63$ \\ \hline
%     \textbf{K-Means Prior} & $0.070 \pm 0.004$ & $83.53 \pm 7.67$ \\ \hline
%     \end{tabular}
%     \caption{Memory Management Overhead per Epoch: $T=20,000$, $W=32$}
%     \label{tab:init_vae_eval}
% \end{table}

% The table \ref{tab:init_vae_eval} shows the random uniform initialisation achieved a marginly lower reconstruction loss of $\sim 0.064$.
% However, the training proved to be much more volatile than the K-Means initialisation method and analysis post-training revealed a average index utilisation.
% This indicates that the VQ-VAE model initialised with random uniform embeddings is subject to the 'initialisation lottery', which can be mitigated by instead initialising codebook embeddings with a data-driven K-Means prior.
% The reduced volatility of training the language ensures high reproducibility for MARL training.

To evaluate the robustness of the VAEs, an ablation study is performed.
Two initialisation strategies are compared:
a standard uniform random initalisation,
and a data-driven K-Means prior.

For the VQ-VAE, the random uniform prior gave superior reconstruction loss and codebook utilisation compared to the K-Means prior.
However, the SQ-VAE exhibited the opposite behaviour, the reconstruction loss and codebook utilisation were superior when using K-Means prior instead of the random uniform prior.

\begin{figure}[H]
    \centering

    \begin{subfigure}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{pca-vq-vae-no-prior.png}
        \caption{VQ-VAE without prior}
        \label{fig:pca-vq-vae-no-prior}
    \end{subfigure}
    \begin{subfigure}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{pca-vq-vae.png}
        \caption{VQ-VAE with prior}
        \label{fig:pca-vq-vae}
    \end{subfigure}

    \begin{subfigure}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{pca-sq-vae-no-prior.png}
        \caption{SQ-VAE without prior}
        \label{fig:pca-sq-vae-no-prior}
    \end{subfigure}
    \begin{subfigure}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{pca-sq-vae.png}
        \caption{SQ-VAE with prior}
        \label{fig:pca-sq-vae}
    \end{subfigure}
    
    \caption{PCA projections of latent codes for SQ-VAE and VQ-VAE with and without the prior.}
    \label{fig:pca-sq-vq-vae}
\end{figure}

To visualise, Principal Component Analysis (PCA) was applied to reduce the dimensionality of the learned codebook embeddings, see figure \ref{fig:pca-sq-vq-vae}.
This visualisation explains the findings.
For VQ-VAE, the random uniform initialisation scattered the codes across the latent space.
This allowed the untrained encoder to naturally intersect with them, resulting in gradients successfully flowing through each of the embeddings.
Conversly, the K-Means prior forced the embeddings into a small area where the untrained encoder would only interact with a select few edge embeddings, leaving a high concentration of unused codes.

For (vMF) SQ-VAE, when using $L_2$-normalised Spherical K-Means prior, the embeddings were each set in the correct region and the model used its stochastic annealing purely for local fine-tuning.
Whereas, when using the random initialisation, embeddings frequently spawned at extreme angular distances from the true data distribution and the model's annealing process pushed the embeddings into a suboptima local minima.

\section{Online MARL Task Performance}
\label{sec:4.4}

\subsection{Baseline Comparisons}
\label{sec:4.4.1}

To assertain the effectiveness of the AI Mother Tongue architecture, it is compared against two other protocols.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{reward_mean_comparison.png}
    \caption{Reward Mean per Step for $30,000$ training steps over PPO training loop}
    \label{fig:reward-mean-comparison}
\end{figure}

TODO: Get confidence intervals

As shown in figure \ref{fig:reward-mean-comparison}, AI Mother Tongue architecture was able to outperform both other protocols.
The No Communication protocol showed the requirement for communication in the simutlation, this protocol converged on a reward mean per step of 0.09.
The Discrete Comms protocol showed a struggle to communicate any useful information from one agent to another and had a similar training convergence to the no communication protocol.

The AI Mother Tongue protocol demonstrates two distinct phases in its training.
The first phase underperforms all the other protocols, as it struggles to balance optimising its auxiliary losses and the standard training losses.
The second phase converges to 0.13 reward mean per step as it learns to use the information stored in order to optimise the auxiliary heads to optimise the standard training losses.

TODO: Talk about optimal, and shortcomings

\subsection{Reflection Strategies - Are They Needed?}
\label{sec:4.4.2}

TODO: Perhaps apply reflection strategies to discrete protocol

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{reflection_comparison.png}
    \caption{Comparison of with and without reflection strategies using Reward Mean per Step for $30,000$ training steps}
    \label{fig:reward-mean-comparison-reflection}
\end{figure}

As shown from figure \ref{fig:reward-mean-comparison-reflection}, Liu et al's reflection strategies are vital for the learning process.
They provide an intermediatery step.
The communication network learns to optimise auxiliary losses, and only once this is done does the actor learn to optimise the standard training losses using the communication network's output.


\section{Information-Theoretic \& Linguistic Properties}
\label{sec:4.5}

\subsection{Spontaneous Semantic Compression}
\label{sec:4.5.1}

Throughout training, communication perplexity of the agent's vocabulary is tracked.
Communication perplexity is measured as $e^{-\text{entropy of vocabulary}}$.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{comm_perplexity_over_time.png}
    \caption{Communication Perplexity tracked over a training run of AI Mother Tongue when using VQ-VAE with a windowed smoothing of 10 steps}
    \label{fig:comm-perplexity-over-training}
\end{figure}

At the start of training, the agents are exploring all possible outputs, leading to a uniform random sampling across the majority of the codebook.
As the RL begins to solve the asymmetric information task (between step 3000 and 7000), communication perplexity collapses and stabilises at $\sim 3.0$.
This collapse represents spontaneous semantic compression.
Instead of using the full theoretical capacity of the codebook, a small subset of codes are assigned useful semantic meaning.
% Do more

\subsection{Codebook Utilisation}
\label{sec:4.5.2}

To further understand the agents use of language, the frequency of each code is analysed.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{vocab-frequency.png}
    \caption{Normalised frequency of each code across 2880 runs}
    \label{fig:vocab-frequency}
\end{figure}

From this data \ref{fig:vocab-frequency}, the distribution exhibits a strong skew giving a low entropy score of 4.24 and a high Gini coefficient of 0.91.
When visualising this empirical distribution with a rank-frequency plot, it demonstrates exponential decay closely resembeling Zipf's Law especially for the first 25 codes.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{rank-frequency-vs-zipf.png}
    \caption{Vocabulary rank-frequency plot compared to Zipf's Law}
    \label{fig:rank-frequency-zipf}
\end{figure}

However, a notable deviation from Zipf's Law does occur at the tail-end of the distribution where the frequency rapidly decreases.
Although sudden drop to zero is not a feature of a classic Zipfian distribution, it is a feature of the truncated Zipfian distribution \cite{TruncatedZipfianDist}.
This occurs because in most languages analysed, an infinite number of words can be created, however, in this system there only exists a finite set of codes.
The emergence of this phenomenon indicates the agents were successful at assigning high frequency tokens to common semantic meaning and low frequency tokens to unnessary tokens.

\section{Cognitive Mapping and Spatial Memory}
\label{sec:4.6}

\subsection{Memory Probing}
\label{sec:4.5.2}

To understand what the agent concretly knows, six strict linear probes were developed following Guillaume et al.'s methodolgy \cite{Probing}.
Each probe takes as input the final layer of the Long-Short Term Memory (LSTM) module, and predicts one of the following using MSE loss:
\begin{itemize}
    \item The agent's own next target's coordinates
    \item The agent's partner's next target's coordinates
    \item The agent's own next target's relative coordinates from the agent's coordinates
    \item The agent's partner's next target's relative coordinates from the agent's coordinates
    \item A unit vector in the direciton of the agent's next target
    \item A unit vector in the direciton of the agent's partner's next target
\end{itemize}

Initially in a simulation run, agent's have no way of knowing the location of their targets.
To account for this, the models are trained only on the last half of the timesteps to ensure the LSTM contains the necassary information for training.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{lstm_probing.png}
    \caption{LSTM Probing Models Test Loss over simulation runs with various tasks}
    \label{fig:lstm-probing}
\end{figure}

The relative distance and unit direction models are shown to never improve over the course of a simulation, suggesting the LSTM does not follow egocentric memory, where information is stored relative from the self (i.e. relative coordinates, or direction to target).
However, the absolute location model shows the LSTM uses allocentric memory, where information is stored using a global coordinates system.

Even though the absolute loaction model does show the LSTM storing some information, the model converges on an MSE of 1.6.
This error represents an uncertainty radius of 1.6 cells, showing how the memory acts as a 'regional beacon' for the agent's targets.

An important conclusion from \ref{fig:lstm-probing} is the agents need for communication decreases as the simulation runs and they store target locations in the LSTM module.

\section{Semantic Grounding and Pragmatics}
\label{sec:4.6}

\subsection{Semantic Grounding: $P(\text{Target} | \text{Code})$}
\label{sec:4.6.1}

Using the conclusion from LSTM Probing (\ref{sec:4.5.2}) of communication is less required as the simulation runs.
Therefore, to analyse semantic meaning of messages, only the first six simulation steps are used.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{partner-target-given-code.png}
    \caption{Liklihood of location of target given the top eight codes broadcasted from partner at different steps in the simulation}
    \label{fig:probability-target-given-code}
\end{figure}

\ref{fig:probability-target-given-code} shows the liklihood of location of target given the code broadcasted from partner.
From this figure, certain codes clearly represent certain regions.
The most prominent is code ten, which always represents the target is in the bottom left quadrant, weighted towards the middle.
This figure also further consolidates the loss of use of communication during a simulation run.
At the inital simulation steps, target locations are clear, however, as the simulation progresses to even step 3, the uncertainty of the location of targets given certain codes increase.

\subsection{Causal Influence on Policy: $P(\text{Action}_{partner}^{t+1} | \text{Code}_{self}^t)$}
\label{sec:4.6.2}

To see if the agent had a direct impact on the action of its partner, a confusion matrix is made:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{partners-action-given-code.png}
    \caption{Probability of partner's action given recieved code within first four simulation steps}
    \label{fig:partner-action-given-code}
\end{figure}

As shown in Figure \ref{fig:partner-action-given-code}, calculating the conditional probability of the receiver's next action given the emitted code reveals a relatively uniform distribution across the action space.
These findings further confirm the findings from the LSTM Probing\ref{sec:4.5.2}, the codes purely communicatie allocentric information.
As each agent navigates to its target independently based off of its own knowlege of the environment, the action taken follows a uniform distribution.

\subsection{Action and Communication Entanglement: $P(\text{Action}_{self}^{t} | \text{Code}_{self}^t)$}
\label{sec:4.6.3}

To understand the relationship between the agent's code and its own action, a confusion matrix is made:

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{own-action-given-code.png}
    \caption{Probability of agent's action given emitted code within first four simulation steps}
    \label{fig:own-action-given-code}
\end{figure}

The figure \ref{fig:own-action-given-code}, which calculate the conditional probability of the next action given a code, is unlike \ref{fig:partner-action-given-code}.
Specifc codes lead to strong probabilities of certain movement actions.
This is a symptom of state-action entanglement.
The action head and communication head for the actor are simple and as they are both derived from the LSTM module, they are strongly correlated.
This leads to the code being outputted by the actor inadvertently encoding its next action choice.


\subsection{Context-Aware Signalling (Inventory-Dependent Language)}
\label{sec:4.6.4}

To understand if the agents are using a pragmatic language, the conditional probability of token usage given the ingredients it has is calculated.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{token-given-inventory.png}
    \caption{Probability of broadcasting token given inventory}
    \label{fig:token-given-inventory}
\end{figure}

As shown in figure \ref{fig:token-given-inventory}, the active vocabulary undergoes large changes depending on the specific items currently held in the agent's inventory.
When the agent has an empty inventory, the vocabulary is highly concentrated on a specific subset of codes.
However, on progression, the vocabulary immediately suppresses these largely used codes.
Once the inventory contains the product, the vocabulary uses a concise set of codes likely used to coordinate drop off location.
These results suggest the agents have a Theory of Mind as intended from the auxiliary losses.
% VAE loss of BCE + 1.75 * MSE


\section{HQ-VAE Analysis}

Replacing the single-level VAE bottleneck with an HQ-VAE did not significantly change task-level performance.
HQ-VAE's goal was for the top level codebook to capture macro-level strategies and the bottom-level codebook to capture micro-level strategies.
To test this, I appied the same analyses as in \ref{sec:4.3} - \ref{sec:4.7} on each codebook and on the bottom-level codebook symbols conditioned on the top-level symbols.
In all cases, no clear, distinguishable strategies emerged at either level.
Further work is required to understand how the agents are using the hierarchical codes, or to stabilise the hierarchy
% Also TODO: Mutual Information analysis, maybe join with LSTM probing